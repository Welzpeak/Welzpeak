{
  "name": "welzpeak",
  "version": "0.0.0",
  "private": true,
  "description": "Document reconciliation engine auditing SA payslips against timecards.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
